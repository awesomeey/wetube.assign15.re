extends layout
include mixin/list

block content
	h1 #{pageTitle} 🤗
	form(method="POST", enctype="multipart/form-data")
		input(type="file", name="txt", accept="text/plain", required)
		input(type="submit", value="Convert!")
	hr
	if errorMsg
		p=errorMsg
	else
		ul
			each file in fileList
				+list(file)